{
  "name": "joshnelson-io",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.4",
    "s3-deploy": "^0.7.3"
  },
  "dependencies": {
    "contentful": "^3.8.0",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.0",
    "styled-components": "^1.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "npm run build && s3-deploy './build/**' --cwd './build/' --region us-east-1 --bucket joshnelson.io",
    "eject": "react-scripts eject"
  }
}
